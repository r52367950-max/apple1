# Codex 并行分工指导 / Parallel Work Guide

> 目标：让 10 个 Codex 并行开发同一项目时“低冲突、强一致、可合并”。
> 每位 Codex 都拥有清晰职责、输入/输出约束与交付检查项。

---

## 总体规则（所有 Codex 必须遵守）

1. **只改自己负责的目录与文件**，不要跨模块调整。
2. **所有文案只来自统一的内容数据文件**（如 `content.ts`），禁止硬编码在组件中。
3. **所有动效严格遵循《项目开发守则》**，禁止弹跳与夸张 easing。
4. **每个模块输出必须是可复用、可维护的组件**，并遵循命名规范。
5. **提交前自检**：组件命名、动效、排版、留白与色彩必须符合 Apple 风格。

---

## Codex-0（总负责人 / 协调者）

**职责**
- 负责全局架构决策、目录结构与依赖策略
- 维护 `content.ts` 文案结构与数据规范
- 最终合并各模块并统一视觉风格

**主要交付**
- 全局 `app/layout`、`globals.css`
- `content.ts` 结构规范
- 合并后的最终页面结构

**提示词**
> 你是项目总负责人。请保持所有组件的数据驱动结构一致，确保每个模块只从 `content.ts` 读取文案，不允许硬编码文本。合并所有模块时，统一节奏与留白，保证页面叙事感与 Apple 风格一致。

---

## Codex-A（Design System & 基础样式）

**职责**
- Tailwind 主题配置（颜色、字号、间距、圆角、阴影）
- 全局排版与基础 Grid 规范

**主要交付**
- `tailwind.config` 主题 Tokens
- `globals.css` 字体栈与基础排版

**提示词**
> 请创建 Apple 风格的设计系统 Tokens，重点在字体、间距、留白与极浅阴影。所有排版层级必须统一可复用。不要添加复杂色彩，主色仅使用黑灰与白。

---

## Codex-B（Hero 首屏）

**职责**
- 首页首屏 Hero 组件
- 视觉主标题、副标题、CTA
- 轻量进入动效

**主要交付**
- `components/sections/ProductHero.tsx`

**提示词**
> 请实现一个 Apple 风格 Hero 区块，主标题与副标题突出克制与高级感。CTA 按钮仅 1–2 个，动效为淡入 + 轻微上移，禁止任何弹跳或夸张缩放。

---

## Codex-C（卖点 Feature 区块）

**职责**
- 3–6 个核心卖点展示
- 小图标或精简图形
- 统一节奏进入动效

**主要交付**
- `components/sections/FeatureGrid.tsx`

**提示词**
> 请实现一个简洁的卖点网格区块。每个卖点必须有标题与简短描述，排版统一，图标极简。动效使用淡入，无夸张 hover。

---

## Codex-D（对比表 Compare Table）

**职责**
- 多款产品规格对比表
- 响应式布局与高可读性

**主要交付**
- `components/sections/CompareTable.tsx`

**提示词**
> 请实现 Apple 风格的对比表，强调清晰排版与留白。表格线条要浅、信息层级明显。避免出现任何促销元素或高饱和色。

---

## Codex-E（叙事段落 / Story Section）

**职责**
- Apple 风格的长文案叙事区块
- 文案与大图交错布局

**主要交付**
- `components/sections/ProductStory.tsx`

**提示词**
> 请实现一个叙事段落区块，强调留白与文字节奏。图文交错排列，动效仅淡入。请保持节奏与 Apple 官网一致，避免信息密集。

---

## Codex-F（购买与转化 CTA）

**职责**
- 价格与购买按钮区块
- 简洁信任说明

**主要交付**
- `components/sections/PurchaseCTA.tsx`

**提示词**
> 请实现购买 CTA 区块，包含价格信息与购买按钮。排版应克制，避免任何促销感。按钮使用统一圆角与轻微 hover。

---

## Codex-G（FAQ / 服务支持）

**职责**
- FAQ 折叠列表
- 售后服务说明

**主要交付**
- `components/sections/FAQ.tsx`

**提示词**
> 请实现 Apple 风格 FAQ 区块，布局简洁、文字易读。交互仅使用轻量展开动画，禁止多重叠加动效。

---

## Codex-H（Footer / 法律信息）

**职责**
- 页脚结构
- 法律与联系信息

**主要交付**
- `components/Footer.tsx`

**提示词**
> 请实现简洁的 Apple 风格页脚。信息分组清晰，使用浅灰分割线。避免任何品牌颜色或营销话术。

---

## Codex-I（动效与交互封装）

**职责**
- 统一 Motion Wrapper
- 统一进入/hover/滚动动效

**主要交付**
- `components/motion/MotionSection.tsx`
- 动效工具函数

**提示词**
> 请创建全局动效封装组件，提供淡入、轻微上移的默认动画，并统一 easing 与 duration。所有动画必须轻量、克制、无弹跳。

---

## Codex-J（内容数据结构与可扩展性）

**职责**
- 全站文案与产品数据结构抽象
- 确保新增产品只需改数据

**主要交付**
- `lib/content.ts` 或 `data/content.ts`

**提示词**
> 请设计一个统一的数据结构用于所有文案与产品信息。禁止在组件中写死文本。新增产品应只需修改数据文件，不改组件。

---

## 交付自检清单（所有 Codex 必须完成）

- [ ] 所有文案来自统一数据文件
- [ ] 排版遵循 Apple 风格留白与层级
- [ ] 动效轻量且不夸张
- [ ] 组件命名与目录规范一致
- [ ] 无跨模块冲突或重复实现

