# 项目开发守则 / Engineering & Design Guidelines

**角色定位**：首席前端架构师 + Apple 官网风格负责人

本守则用于指导多个 Codex 并行开发中性笔电商官网，确保视觉一致、工程一致与协作无缝合并。所有实现需满足 Apple 官网风格的精致感、克制感与高品质动效，避免电商化“促销感”。

---

## 1. 项目目标与设计哲学

### 1.1 目标定义：什么是“像 Apple”
- **克制与高级**：视觉克制、留白充足、信息有呼吸感，强调产品本身。
- **清晰层级**：清晰的标题/正文/辅助信息层级，减少视觉噪音。
- **高质感动效**：动效为提升质感与引导而服务，非炫技。
- **聚焦体验**：滑动节奏、版式节奏一致，页面具有“叙事感”。

### 1.2 明确禁止项：什么是“像廉价电商”
- 禁止大面积高饱和促销色、闪烁动画、跑马灯、弹跳式按钮。
- 禁止“堆砌信息”的展示，特别是过量价格信息、折扣标签、活动倒计时。
- 禁止过度拟物、低质量阴影、复杂渐变背景。
- 禁止廉价字体和无统一系统的字号/行高。

### 1.3 页面节奏、留白、信息密度原则
- **节奏**：每个区块之间必须有明确的间隔（Vertical rhythm），避免“黏连”。
- **留白**：大面积留白是默认，内容块应“漂浮”于空间中。
- **信息密度**：一屏仅承载一个核心信息点；多点信息以层级和排版分段。

---

## 2. 视觉与排版规范（Design System Rules）

### 2.1 字体栈（中文 / 英文 / 数字）
- **中文**：`"PingFang SC"`，后备：`"Helvetica Neue"`, `"Microsoft YaHei"`, `sans-serif`
- **英文/数字**：`"Helvetica Neue"`, `"SF Pro Display"`, `"SF Pro Text"`, `sans-serif`
- **原则**：统一使用系统级字体，避免引入第三方衬线或装饰字体。

### 2.2 字号与行高规范
- **标题 H1**：56–64px / 行高 1.05–1.1
- **标题 H2**：40–48px / 行高 1.1–1.2
- **标题 H3**：28–32px / 行高 1.2–1.3
- **正文**：17–19px / 行高 1.6–1.8
- **注释/辅助**：12–14px / 行高 1.5–1.6
- **规则**：同一页面仅使用 3–4 种字号层级，避免字号过多。

### 2.3 颜色使用规则
- **主色**：近黑灰 (`#111111` / `#1D1D1F`) 与纯白 (`#FFFFFF`)
- **辅助色**：浅灰 (`#F5F5F7` / `#EDEDED`)；产品点缀色需小面积使用。
- **禁止颜色**：高饱和红/橙/黄；过度渐变；低对比灰字。
- **规则**：颜色仅用于强调层级与交互状态，不作为装饰。

### 2.4 圆角、阴影、边框、分割线规范
- **圆角**：统一 8px（小组件）、12px（卡片）或 16px（大容器）
- **阴影**：极浅、扩散大、透明度低（如 0 6px 30px rgba(0,0,0,0.08)）
- **边框**：仅用于分隔（1px，`#D2D2D7` 或更淡）
- **分割线**：使用低对比灰线，避免醒目色

---

## 3. 动效与交互守则（Animation & Interaction Rules）

### 3.1 允许的动效类型
- **进入动效**：淡入 + 轻微位移（Y 8–16px）
- **Hover 动效**：轻微升起或阴影增强，缩放幅度 ≤ 1.02
- **滚动动效**：渐进式呈现与视差（轻量），必要时使用 Framer Motion / GSAP
- **交互反馈**：点击时仅轻微缩放或透明度变化

### 3.2 动效时长与缓动
- **短动效**：180–240ms（按钮、hover）
- **中动效**：360–520ms（模块出现、图片进场）
- **长动效**：600–900ms（关键叙事段落）
- **推荐缓动**：`easeOut`, `cubic-bezier(0.16, 1, 0.3, 1)`

### 3.3 明确禁止的动效风格
- 禁止弹跳/震动/快速闪烁式动效
- 禁止夸张缓动（overshoot、bounce）
- 禁止多重叠加动画造成“廉价感”

---

## 4. 页面与组件结构规范

### 4.1 页面目录结构（Next.js App Router）
- `app/`：页面入口与路由
- `components/`：通用组件与布局模块
- `components/sections/`：页面级大模块（Hero、Feature、Compare）
- `components/ui/`：原子组件（Button、Badge、Icon）
- `lib/`：工具函数与数据抽象

### 4.2 组件命名规则
- **页面模块**：`ProductHero`, `FeatureGrid`, `CompareTable`
- **可复用组件**：`ProductCard`, `SpecList`, `PurchaseCTA`
- **原子组件**：`Button`, `Tag`, `Divider`, `Icon`

### 4.3 页面级组件 vs 原子组件边界
- **页面级组件**：承担布局与内容组织，不包含复杂的业务逻辑
- **原子组件**：提供基础 UI 能力，禁止直接耦合页面数据

---

## 5. 多人协作与并行开发规范

### 5.1 模块独立开发原则
- 每个 Codex 仅修改自己负责模块目录，避免跨模块变更。
- 所有共用样式与设计 token 必须集中定义，禁止在模块中重复。

### 5.2 公共组件 vs 私有组件
- **公共组件**：放入 `components/` 或 `components/ui/`，需附注用途与限制。
- **私有组件**：仅在模块内部使用，必须放在模块文件夹中。

### 5.3 提交前自检清单（Checklist）
- 视觉规范与排版规则已遵守
- 动效符合允许范围
- 未引入多余依赖
- 代码风格一致，命名符合约定
- 与其他模块无冲突、无重复实现

---

## 6. 性能、可维护性与扩展性原则

### 6.1 图片与资源
- 图片必须压缩并使用现代格式（如 WebP）
- 仅使用高质量产品图，避免低清或拉伸

### 6.2 动画与状态管理
- 动画必须有性能预算，避免大型 DOM 频繁重排
- 状态管理仅在必要时引入；组件优先保持无状态

### 6.3 扩展与新增产品
- 新增产品仅通过数据驱动，不破坏现有结构
- 禁止为单一产品写“特例”样式

---

## 7. 验收标准

### 7.1 通过评审条件
- 页面在 Safari（iPad）下显示与动效无误
- 视觉风格与 Apple 官网一致（克制、高级、整洁）
- 组件结构清晰、可维护、可复用
- 与其他模块无冲突，易于合并

### 7.2 必须返工条件
- 出现明显电商促销感（高饱和、堆信息、夸张动效）
- 排版拥挤、留白不足、字体不统一
- 动效浮夸或性能低下
- 结构混乱、重复组件或不符合命名规范

---

**结语**：本守则即为准入标准。若对某一规范存在歧义，默认选择“更克制、更简洁、更像 Apple”的实现方式。
